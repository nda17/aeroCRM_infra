# Included only in the HTTPS telegram.aerocrm.space server.
location = /api/v1/telegram-bot/webhook {
  limit_except POST { deny all; }

  proxy_pass https://api.aerocrm.space;
  proxy_ssl_server_name on;
  proxy_ssl_name api.aerocrm.space;
  proxy_ssl_verify on;
  proxy_ssl_trusted_certificate /etc/ssl/certs/ca-certificates.crt;
  proxy_ssl_verify_depth 3;
  proxy_http_version 1.1;
  proxy_set_header Host api.aerocrm.space;
  proxy_set_header X-Real-IP $remote_addr;
  proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  proxy_set_header X-Forwarded-Host $host;
  proxy_set_header X-Forwarded-Proto $scheme;
  proxy_set_header X-Telegram-Bot-Api-Secret-Token $http_x_telegram_bot_api_secret_token;
  proxy_request_buffering off;
  proxy_buffering off;
  proxy_connect_timeout 5s;
  proxy_read_timeout 15s;
  proxy_send_timeout 15s;
}

location = /api/v1/telegram-bot/support-webhook {
  limit_except POST { deny all; }

  proxy_pass https://api.aerocrm.space;
  proxy_ssl_server_name on;
  proxy_ssl_name api.aerocrm.space;
  proxy_ssl_verify on;
  proxy_ssl_trusted_certificate /etc/ssl/certs/ca-certificates.crt;
  proxy_ssl_verify_depth 3;
  proxy_http_version 1.1;
  proxy_set_header Host api.aerocrm.space;
  proxy_set_header X-Real-IP $remote_addr;
  proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  proxy_set_header X-Forwarded-Host $host;
  proxy_set_header X-Forwarded-Proto $scheme;
  proxy_set_header X-Telegram-Bot-Api-Secret-Token $http_x_telegram_bot_api_secret_token;
  proxy_request_buffering off;
  proxy_buffering off;
  proxy_connect_timeout 5s;
  proxy_read_timeout 15s;
  proxy_send_timeout 15s;
}
